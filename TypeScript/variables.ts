let greeting: string = "Hello!";

let id: number = 50;

let isLog: boolean = true;

let tab: string[] = ["thor", "spider-man", "ironman"];

const userObject = {
    name: "Oliwia",
    email: "test@email.com",
    isActive: true
}

type Alias = {
    name: string,
    email: string,
    isActive: boolean,
    walk: () => void
}